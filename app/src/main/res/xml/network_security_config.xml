<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Dev: cleartext allowed everywhere.
        Before Play Store release, change base-config back to cleartextTrafficPermitted="false"
        and keep only the production HTTPS domain.
    -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
